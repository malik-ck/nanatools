% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/superlearning.R
\name{train_ensemble}
\alias{train_ensemble}
\title{Fit a cross-validated SuperLearner}
\usage{
train_ensemble(init, cv_instructions, x, y, return_data = TRUE)
}
\arguments{
\item{init}{An initialization object of class \code{Ensemble_Setup} created
via \code{setup_ensemble()}.}

\item{cv_instructions}{Either a \code{nana_cv} object created via
\code{create_cv_folds()} or \code{add_cv_folds()}, or an integer vector
of length 2 giving \code{c(inner_cv, outer_cv)} fold counts (either may
be \code{NA} or \code{NULL} to indicate no CV at that level).}

\item{x}{A data frame or matrix of predictor variables.}

\item{y}{A numeric vector of outcome values.}

\item{return_data}{Logical. If \code{TRUE} (default), \code{x} and
\code{y} are stored in the returned object, enabling standalone
prediction without re-supplying data. Set to \code{FALSE} in pipeline
use where the task is the source of truth.}
}
\value{
An S3 object of class \code{Ensemble}.
}
\description{
Fit a cross-validated SuperLearner
}
