% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initiate_study.R
\name{initiate_study}
\alias{initiate_study}
\title{Initiate a causal inference study task}
\usage{
initiate_study(
  data,
  treatment,
  confounders,
  outcomes,
  time = NULL,
  cluster = NULL,
  treatment_labels = NULL,
  confounder_labels = NULL,
  outcome_labels = NULL,
  time_labels = NULL,
  cluster_labels = NULL,
  adjustment_sets = NULL,
  extra_vars = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{matrix} containing all study
variables. Used only during construction; not stored in the returned task.}

\item{treatment}{<\code{\link[tidyselect:language]{tidyselect}}> Treatment column(s).
Also accepts a character vector of column names or an integer index vector.
All treatment columns must be numeric or integer; binary treatments must
already be coded as 0/1. Stored as a matrix or data frame matching the
class of \code{data}.}

\item{confounders}{<\code{\link[tidyselect:language]{tidyselect}}> Global adjustment
set. Stored as a matrix or data frame.}

\item{outcomes}{A fully named \code{list}. Each element identifies one study
outcome via a character vector of column names or an integer index vector.
Single-column outcomes are stored as plain vectors; multi-column outcomes
are stored as a matrix or data frame.}

\item{time}{<\code{\link[tidyselect:language]{tidyselect}}> Optional time variable(s).}

\item{cluster}{<\code{\link[tidyselect:language]{tidyselect}}> Optional cluster
variable(s).}

\item{treatment_labels}{Optional character vector of display labels for
treatment columns. Named: matched by column name. Unnamed: positional.}

\item{confounder_labels}{Optional character vector of display labels for
confounder columns. Named or unnamed (positional).}

\item{outcome_labels}{Optional character vector of display labels for
outcomes. Named: matched by outcome list name. Unnamed: positional.}

\item{time_labels}{Optional character vector of display labels for time
columns. Named or unnamed (positional).}

\item{cluster_labels}{Optional character vector of display labels for
cluster columns. Named or unnamed (positional).}

\item{adjustment_sets}{Optional named list (names must be a subset of
\code{names(outcomes)}). Each element is either a character vector of
confounder column names or an integer index vector into the columns of the
stored confounder block. \code{NULL} entries (or omitted outcomes) inherit
the full global confounder set.}

\item{extra_vars}{Optional named list of arbitrary objects to be carried in
the task and made available to downstream functions (e.g. penalty matrices,
offset vectors, external data).}

\item{verbose}{Logical. If \code{TRUE} (default), pipeline functions emit
informational messages.}
}
\value{
An S3 object of class \code{nana_task}.
}
\description{
Initiate a causal inference study task
}
