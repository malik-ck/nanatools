% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/superlearning_methods.R
\name{predict.Ensemble}
\alias{predict.Ensemble}
\title{Predict from a fitted SuperLearner}
\usage{
\method{predict}{Ensemble}(
  object,
  newdata = NULL,
  metalearner_name = NULL,
  output_best = FALSE,
  ensemble_fold_id = NULL,
  type = NULL,
  ...
)
}
\arguments{
\item{object}{A fitted \code{Ensemble} object.}

\item{newdata}{New predictor data. For \code{type = "cv"}, defaults to the
stored training data and predictions are generated on each outer fold's
validation set. For \code{type = "ensemble"}, predictions are generated
on the full \code{newdata}.}

\item{metalearner_name}{Character vector of metalearner (or learner) names
to return predictions for. If \code{NULL} and \code{output_best = FALSE},
all metalearners and learners are returned.}

\item{output_best}{Logical. If \code{TRUE} and \code{metalearner_name} is
\code{NULL}, returns only the best-performing metalearner. Requires outer
CV.}

\item{ensemble_fold_id}{Integer. For \code{type = "ensemble"} with outer CV,
selects which fold's fitted ensemble to use for prediction.}

\item{type}{Character. One of \code{"cv"} or \code{"ensemble"}.}

\item{...}{Ignored.}
}
\value{
For a single metalearner: a prediction object of the type returned
by that learner, with an \code{indices} attribute for \code{type = "cv"}.
For multiple metalearners: a named list of such objects.
}
\description{
Predict from a fitted SuperLearner
}
